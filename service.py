# 此处填写业务逻辑

import MercurySQLite
import time

def init_db(db: MercurySQLite.DataBase):
    # table = db['heartbeat']
    # table.newColumn('id', int, primaryKey=True)
    # # table.newColumn('time', str)
    # print(table.columns)

    # 初始化数据库的操作
    ...

def do_heartbeat(db: MercurySQLite.DataBase):
    # 插入heartbeat新的数据
    # table = db['heartbeat']
    # table.insert(id=1)
    # # 返回最新的数据
    # return table[-1]

    # 返回heartbeat的最新数据
    ...